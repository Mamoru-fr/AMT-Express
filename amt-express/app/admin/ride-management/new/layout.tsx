export const metadata = {
    title: "New Ride | AMT Express",
    description: "Create a new ride"
};

export default function NewRideLayout({children}: {children: React.ReactNode}) {
    return children;
}
